import { AuthLink, AuthOptions, AUTH_TYPE } from './auth-link';
export declare const createAuthLink: ({ url, region, auth }: {
    url: string;
    region: string;
    auth: AuthOptions;
}) => AuthLink;
export { AuthLink, AuthOptions, AUTH_TYPE };
export { Signer } from './signer';
